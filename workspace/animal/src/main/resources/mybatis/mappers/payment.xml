<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.payment">

    <!-- 1. payment 테이블에 결제 기본 정보 저장 -->
    <insert id="insertPayment" parameterType="paymentDTO">
      INSERT INTO payment (
        payment_id,
        member_id,
        total_amount,
        payment_date
      ) VALUES (
        payment_seq.NEXTVAL,  <!-- 시퀀스 사용 -->
        #{memberId},
        #{totalAmount}, 
        SYSDATE
      )
    </insert>

    <!-- 2. payment_detail 테이블에 상품별 상세 저장 -->
    <insert id="insertPaymentDetail" parameterType="paymentDetailDTO">
        <![CDATA[
        INSERT INTO payment_detail (
            payment_id, 
            product_id, 
            quantity, 
            price, 
            total_amount, 
            product_name, 
            payment_time
        ) VALUES (
            #{paymentId, jdbcType=VARCHAR}, 
            #{productId}, 
            #{quantity}, 
            #{price}, 
            #{totalAmount}, 
            #{productName}, 
            sysdate
        )
        ]]>
    </insert>

</mapper>
