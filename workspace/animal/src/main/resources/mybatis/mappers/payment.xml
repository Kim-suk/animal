<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.payment">

    <!-- 결제 정보 저장 -->
    <insert id="insertPayment" parameterType="com.test.animal.payment.dto.PaymentDTO">
        INSERT INTO payment (order_number, user_id, total_amount, payment_method, payment_status, payment_date)
        VALUES (#{orderNumber}, #{userId}, #{totalAmount}, #{paymentMethod}, #{paymentStatus}, NOW())
    </insert>

    <!-- 결제 상태 업데이트 -->
    <update id="updatePaymentStatus" parameterType="com.test.animal.payment.dto.PaymentDTO">
        UPDATE payment
        SET payment_status = #{paymentStatus}
        WHERE order_number = #{orderNumber}
    </update>

    <!-- 주문 번호로 결제 정보 가져오기 -->
    <select id="getPaymentByOrderNumber" resultType="com.test.animal.payment.dto.PaymentDTO">
        SELECT * FROM payment
        WHERE order_number = #{orderNumber}
    </select>

    <!-- 사용자 ID로 결제 내역 조회 -->
    <select id="getPaymentsByUserId" resultType="com.test.animal.payment.dto.PaymentDTO">
        SELECT * FROM payment
        WHERE user_id = #{userId}
        ORDER BY payment_date DESC
    </select>

</mapper>
