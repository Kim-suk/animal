<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.member">

	<resultMap type="com.test.animal.member.dto.MemberDTO"
		id="memResult">
		<result property="id" column="id" />
		<result property="pwd" column="pwd" />
		<result property="age" column="age" />
		<result property="name" column="name" />
		<result property="gender" column="gender" />
		<result property="email" column="email" />
		<result property="joinDate" column="joinDate" />
		<result property="naverId" column="naver_id" />
		<result property="googleId" column="google_id" />
		<result property="kakaoId" column="kakao_id" />
		<result property="facebookId" column="facebook_id" />
		<result property="profileImg" column="profile_img" />
		<result property="joinType" column="join_type" />
		<result property="nickname" column="nickname" />
	</resultMap>

	<!-- 아이디 중복 체크 -->
	<select id="checkUserId" parameterType="java.lang.String"
		resultType="int">
        <![CDATA[
            SELECT COUNT(*) FROM animal_member WHERE id = #{id}
        ]]>
	</select>

	<!-- 전체 회원 리스트 -->
	<select id="listMembers" resultMap="memResult">
        <![CDATA[
            SELECT * FROM animal_member ORDER BY joinDate DESC
        ]]>
	</select>

	<!-- 회원 상세 -->
	<select id="memberDetail" parameterType="java.lang.String"
		resultType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            SELECT * FROM animal_member WHERE id = #{id}
        ]]>
	</select>

	<!-- 회원 삭제 -->
	<delete id="delMember" parameterType="java.lang.String">
        <![CDATA[
            DELETE FROM animal_member WHERE id = #{id}
        ]]>
	</delete>

	<!-- 일반 회원 가입 -->
	<insert id="addMember"
		parameterType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            INSERT INTO animal_member (
                id, pwd, age, name, gender, email, joinDate
            ) VALUES (
                #{id}, #{pwd}, #{age}, #{name}, #{gender}, #{email}, SYSDATE
            )
        ]]>
	</insert>

	<!-- 회원 수정 -->
	<update id="changePwd" parameterType="map">
		UPDATE animal_member
		SET pwd = #{pwd}
		WHERE id = #{id}
	</update>

	<!-- 로그인 -->
	<select id="login"
		parameterType="com.test.animal.member.dto.MemberDTO"
		resultType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            SELECT * FROM animal_member WHERE id = #{id} AND pwd = #{pwd}
        ]]>
	</select>

	<!-- 사용자 조회 -->
	<select id="selectByUserId" parameterType="java.lang.String"
		resultType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            SELECT * FROM animal_member WHERE id = #{id}
        ]]>
	</select>

	<!-- 이메일로 회원 조회 -->
	<select id="selectByEmail" parameterType="java.lang.String"
		resultMap="memResult">
    <![CDATA[
        SELECT * FROM animal_member WHERE email = #{email, jdbcType=VARCHAR}
    ]]>
	</select>

	<select id="selectByGoogleId" parameterType="java.lang.String"
		resultMap="memResult">
        <![CDATA[
            SELECT * FROM animal_member WHERE google_id = #{googleId, jdbcType=VARCHAR}
        ]]>
	</select>

	<select id="selectByKakaoId" parameterType="java.lang.String"
		resultMap="memResult">
        <![CDATA[
            SELECT * FROM animal_member WHERE kakao_id = #{kakaoId, jdbcType=VARCHAR}
        ]]>
	</select>

	<!-- 네이버 ID로 회원 조회 -->
	<select id="selectByNaverId" parameterType="java.lang.String"
		resultMap="memResult">
    <![CDATA[
        SELECT * FROM animal_member WHERE naver_id = #{naverId, jdbcType=VARCHAR}
    ]]>
	</select>

	<select id="selectByFacebookId" parameterType="java.lang.String"
		resultMap="memResult">
    <![CDATA[
        SELECT * FROM animal_member WHERE facebook_id = #{facebookId, jdbcType=VARCHAR}
    ]]>
	</select>


	<insert id="insertNaverUser"
		parameterType="com.test.animal.member.dto.MemberDTO">
    <![CDATA[
        INSERT INTO animal_member (
            id, pwd, age, name, gender, email, nickname, join_type, naver_id, joinDate
        ) VALUES (
            #{id}, #{pwd}, #{age}, #{name}, #{gender}, #{email}, #{nickname},
            #{joinType}, #{naverId, jdbcType=VARCHAR}, SYSDATE
        )
    ]]>
	</insert>
	<insert id="insertGoogleUser"
		parameterType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            INSERT INTO animal_member (
                id, pwd, name, email, google_id, join_type, age, gender, joinDate
            ) VALUES (
                #{id}, #{pwd}, #{name}, #{email}, #{googleId, jdbcType=VARCHAR}, #{joinType}, 0, 'U', SYSDATE
            )
        ]]>
	</insert>

	<insert id="insertKakaoUser"
		parameterType="com.test.animal.member.dto.MemberDTO">
       
            <![CDATA[
        INSERT INTO ANIMAL_MEMBER
        (id, pwd, age, name, gender, email, join_Type, kakao_id, nickname)
        VALUES
        (#{id}, #{pwd}, #{age}, #{name}, #{gender}, #{email}, #{joinType}, #{kakaoId, jdbcType=VARCHAR}, #{nickname})
    ]]>

	</insert>

	<insert id="insertFacebookUser"
		parameterType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            INSERT INTO animal_member (
                id, pwd, name, email, facebook_id, join_type, age, gender, joinDate
            ) VALUES (
                #{id}, #{pwd}, #{name}, #{email}, #{facebookId, jdbcType=VARCHAR}, #{joinType}, 0, 'U', SYSDATE
            )
        ]]>
	</insert>

	<select id="checkEmailExists" parameterType="java.lang.String"
		resultType="int">
        <![CDATA[
            SELECT COUNT(*) FROM animal_member WHERE email = #{email}
        ]]>
	</select>

	<insert id="registerKakaoUser"
		parameterType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            INSERT INTO animal_member (
                id, pwd, age, name, gender, joinDate, kakao_id, join_type, nickname
            ) VALUES (
                #{id}, #{pwd}, #{age}, #{name}, #{gender}, SYSDATE, #{kakaoId, jdbcType=VARCHAR}, #{joinType}, #{nickname}
            )
        ]]>
	</insert>

	<insert id="registerNaverUser"
		parameterType="com.test.animal.member.dto.MemberDTO">
        <![CDATA[
            INSERT INTO animal_member (
                id, pwd, age, name, gender, joinDate, naver_id, join_type, nickname
            ) VALUES (
                #{id}, #{pwd}, #{age}, #{name}, #{gender}, SYSDATE, #{naverId, jdbcType=VARCHAR}, #{joinType}, #{nickname}
            )
        ]]>
	</insert>

	<update id="updateNaverId"
		parameterType="com.test.animal.member.dto.MemberDTO">
     <![CDATA[
        UPDATE animal_member
        SET naver_id = #{naverId},
            join_type = #{joinType},
            nickname = #{nickname}
        WHERE id = #{id}
    ]]>
	</update>

	<select id="getPasswordByUserId" parameterType="String"
		resultType="String">
		SELECT pwd FROM animal_member WHERE id = #{id}
	</select>

	<update id="updatePassword" parameterType="map">
		UPDATE animal_member SET pwd = #{pwd} WHERE id = #{id}
	</update>

	<update id="updateProfileImage"
		parameterType="com.test.animal.member.dto.MemberDTO">
		UPDATE animal_member SET profile_img = #{profileImg} WHERE id = #{id}
	</update>

	<select id="validateOldPwd" resultType="int" parameterType="map">
		SELECT COUNT(*)
		FROM animal_member
		WHERE id = #{id} AND pwd = #{pwd}
	</select>
	
	 <!-- 회원 정보 조회 -->
    <select id="selectById" resultType="com.test.animal.member.dto.MemberDTO">
        SELECT * FROM animal_member WHERE id = #{id}
    </select>
    
     <!-- 회원 정보 수정 -->
    <update id="updateMember" parameterType="com.test.animal.member.dto.MemberDTO">
        UPDATE animal_member
        SET name = #{name},
            age = #{age},
            gender = #{gender},
            email = #{email}
        WHERE id = #{id}
    </update>
    
    <!-- 회원 삭제 -->
	<delete id="deleteMember" parameterType="string">
    	DELETE FROM animal_member
		WHERE id = #{id}
	</delete>
	
	<!-- 프로필 업로드 -->
	<update id="changeProfileImage" parameterType="com.test.animal.member.dto.MemberDTO">
    UPDATE animal_member
    SET PROFILE_IMG = #{profileImg}
    WHERE id = #{id}
</update>
	
    <!-- 프로필 이미지 기본 이미지로 초기화 -->
<update id="deleteProfileImage" parameterType="String">
    UPDATE animal_member
    SET profile_Img = NULL
    WHERE id = #{id}
</update> 
</mapper>