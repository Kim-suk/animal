<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.animalHospital">

		<insert id="batchInsertHospitals">
			<foreach collection="list" item="hospital" open="insert all" separator=" " close="select * from dual">
				into animal_hospital (id, name, address, phone, latitude, longitude)
				values (#{hospital.id}, #{hospital.name},#{hospital.address},#{hospital.phone}, #{hospital.latitude}, #{hospital.longitude}) 
			</foreach>
		</insert>








</mapper>