<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.cat_board">
	<resultMap type="articleDTO" id="artResult">
		<result property="articleNo" column="articleNo" />
		<result property="parentNo" column="parentNo" />
		<result property="title" column="title" />
		<result property="content" column="content" />
		<result property="writeDate" column="writeDate" />
		<result property="imageFileName" column="imageFileName" />
		<result property="id" column="id" />
	</resultMap>
	
	<resultMap type="imageDTO" id="imgResult">
		<result property="imageFileNo" column="imageFileNo"/>
		<result property="imageFileName" column="imageFileName" />
		<result property="regDate" column="regDate" />
		<result property="articleNo" column="articleNo" />
	</resultMap>
	
	<select id="cat_selectAllArticlesList" resultMap="artResult">
		<![CDATA[
			select articleNo, parentNo, title, content, writeDate, 
			imageFileName, id from cat_board 
			start with parentNo = 0
			connect by prior articleNo=parentNo
			order siblings by articleNo desc
		]]>
	</select>
	
	<insert id="cat_insertNewArticle" parameterType="articleDTO">
		<![CDATA[
			insert into cat_board values(#{articleNo}, #{parentNo}, 
			#{title}, #{content}, null, #{id}, sysdate )
		]]>
	</insert>
	
	<select id="cat_selectNewArticleNo" resultType="Integer">
		<![CDATA[
			select NVL(max(articleNo), 0) + 1 from cat_board
		]]>
	</select>
	
	<select id="cat_selectArticle" resultType="articleDTO" parameterType="int">
		<![CDATA[
			select * from cat_board where articleNo=#{articleNo}
		]]>
	</select>
	
	<update id="cat_updateArticle" parameterType="java.util.Map">
		update cat_board 
		set title=#{title}, content=#{content}
		<if test="imageFileName != '' and imageFileName != null">
			, imageFileName=#{imageFileName}
		</if>
		where articleNo=#{articleNo}
	</update>
	
	<delete id="cat_deleteArticle" parameterType="int">
		<![CDATA[
			delete from cat_board where articleNo in 
			(select articleNo from cat_board start with articleNo=#{articleNo}
			connect by prior articleNo=parentNo)
		]]>
	</delete>
	
	<insert id="cat_insertNewImage" parameterType="java.util.Map">
		<foreach item="item" collection="list" open="insert all"
			separator=" " close="select * from dual">
			into image(imageFileNo, imageFileName, articleNo, regDate)
			values (#{item.imageFileNo}, #{item.imageFileName}, #{item.articleNo},
			sysdate)	
		</foreach>
	</insert>
	
	<select id="cat_selectNewImageFileNo" resultType="int">
		<![CDATA[
			select nvl(max(imageFileNo), 0) from image
		]]>
	</select>
	
	<select id="cat_selectImageFileList" resultMap="imgResult">
		<![CDATA[
			select * from image where articleNo=#{articleNo}
		]]>
	</select>
	
	<delete id="cat_deleteImage" parameterType="int">
		<![CDATA[
			delete from image where imageFileNo=#{imageFileNo}
		]]>	
	</delete>
	
	<select id="cat_selectArticleNo" parameterType="int" resultType="int">
		<![CDATA[
			select articleNo from image where imageFileNo=#{imageFileNo}
		]]>
	</select>
</mapper>






