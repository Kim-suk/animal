<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
	"http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="mapper.animalHospital">

	<resultMap type="animalHospitalDTO" id="hospitalResultMap" >
		<id property="id" column="id" />
		<result property="name" column="name" />
		<result property="address" column="address" />
		<result property="phone" column="phone" />
		<result property="latitude" column="latitude" />
		<result property="longitude" column="longitude" />
	</resultMap>

	<insert id="insertHospitals">
		<foreach collection="list" item="hospital" open="insert all"
			separator=" " close="select * from dual">
			into animal_hospital (id, name, address, phone,
			latitude, longitude)
			values (#{hospital.id},
			#{hospital.name},#{hospital.address},#{hospital.phone},
			#{hospital.latitude}, #{hospital.longitude})
		</foreach>
	</insert>

<select id="getAllHospitals" resultType="animalHospitalDTO">
    SELECT 
        id, name, address, phone, latitude, longitude
    FROM 
        animal_hospital
    WHERE 
        latitude IS NOT NULL AND longitude IS NOT NULL
</select>
    
    
    <select id="getHospitalById" parameterType="int" resultType="animalHospitalDTO">
        SELECT id, name, address, phone, latitude, longitude
        FROM animal_hospital
        WHERE id = #{id}
    </select>










</mapper>